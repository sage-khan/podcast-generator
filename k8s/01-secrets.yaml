# Template for the app-secrets Kubernetes Secret.
#
# Do NOT put real values in this file — it is committed to the public repo.
# Instead:
#   1. Copy this file locally (e.g. `k8s/01-secrets.local.yaml`, already gitignored)
#      and fill in real values, OR
#   2. Generate the secret directly from your `.env` file:
#        kubectl create secret generic app-secrets -n podcast-generator \
#          --from-env-file=.env --dry-run=client -o yaml | kubectl apply -f -
#
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: podcast-generator
type: Opaque
stringData:
  MANAGED_POSTGRES_PASSWORD: "REPLACE_ME"
  MANAGED_POSTGRES_USER: "REPLACE_ME"
  MANAGED_POSTGRES_DB: "REPLACE_ME"
  MANAGED_PG_HOST: "REPLACE_ME"
  MANAGED_PG_PORT: "REPLACE_ME"
  MANAGED_PG_DATABASE_URL: "REPLACE_ME"
  MANAGED_REDIS_PASSWORD: ""
  MANAGED_REDIS_HOST: ""
  MANAGED_REDIS_PORT: ""
  REPLICATE_API_TOKEN: "REPLACE_ME"
  HF_TOKEN: "REPLACE_ME"
  DJANGO_SECRET_KEY: "REPLACE_ME"
  DJANGO_SUPERUSER_USERNAME: "REPLACE_ME"
  DJANGO_SUPERUSER_PASSWORD: "REPLACE_ME"
  DJANGO_SUPERUSER_EMAIL: "REPLACE_ME"
  GITHUB_PAT: "REPLACE_ME"
  DO_SPACES_KEY: "REPLACE_ME"
  DO_SPACES_SECRET: "REPLACE_ME"
  DO_API_TOKEN: "REPLACE_ME"
  DO_KUBERNETES_TOKEN: "REPLACE_ME"
  K8S_CERTIFICATE_AUTHORITY_DATA: "REPLACE_ME"
  DO_SPACES_ENDPOINT: "https://nyc3.digitaloceanspaces.com"
  AWS_ACCESS_KEY: "REPLACE_ME"
  AWS_SECRET_KEY: "REPLACE_ME"
